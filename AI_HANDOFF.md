# AI Handoff

## Objective

Build a browser-based photography practice game for beginners that teaches the exposure triangle: realistic camera controls with visible consequences for shutter speed, aperture, and ISO (plus focal length as a framing control).

## Current State

- UI refinement completed on 2026-08-08: changed the shooting controls to a phone-camera-style right rail on desktop. The shutter is now a fixed red control at the right-center, the photo library remains at the far lower-right, and the desktop exposure dock plus mobile exposure console use transparent backgrounds with text shadows for legibility. Live visual checks passed at 1280×720 and 390×844: the desktop shutter rendered at right-center, the library at the lower-right, both exposure-control surfaces computed to transparent, the mobile layout had no horizontal overflow, and the browser console was clean. Production build, all 13/13 tests, and `git diff --check` passed.

- Small UI refinement completed on 2026-08-08: moved the desktop photo-library control from the lower-left to the lower-right of the camera dock by swapping it with the shutter control. Replaced the abstract grid glyph on desktop and mobile with a CSS-drawn photo-thumbnail pictogram. Production build, all 13/13 tests, and `git diff --check` passed.

- Small UI refinement completed on 2026-08-08: made the expanded exposure-scale panel fully transparent so the 3D scene remains visible behind its ruler and labels. Removed the panel shadow and added text shadow for legibility; the solid camera-body bottom dock remains unchanged. Production build, all 13/13 tests, and `git diff --check` passed.

- Active local objective completed on 2026-08-08: fully redesigned the in-scene camera UI for visual quality and readability. The implementation now uses a minimal scene/mode top HUD and a camera-back-style bottom dock whose only setting readouts are shutter, aperture, ISO, and exposure compensation. Clicking any of the four opens a large scale above the dock; horizontal mouse movement or dragging changes the selected value. The chosen parameter automatically moves to an exposure mode that permits direct adjustment. The old persistent mission/progress/status/help/lens readouts and the in-panel photo strip/lesson were removed. Capture now flashes and saves without opening playback or dropping pointer lock. A dedicated photo-library button opens a full-screen gallery, and individual playback is reachable only from that gallery. Mobile quick controls now mirror the same four exposure settings and use a separate library button.
  - Live browser verification passed at 1280×720 and 375×812: all four exposure controls opened the correct scale and changed values through horizontal pointer movement; capture saved a photo while leaving live view visible; the library badge updated; desktop and mobile galleries rendered without horizontal overflow; individual playback was reachable only from the gallery and returned to it correctly; and the browser console had no errors or warnings. This pass found and fixed a real playback-layer bug where the photo covered the return button because the base `.playback` positioning rule was absent.
  - Final validation passed on 2026-08-08: production build succeeded, all 13/13 product tests passed, and `git diff --check` passed. The existing Three.js client chunk still emits Vite's >500 kB warning. No commit, push, or deployment was performed.

- New local objective on 2026-08-07: move camera exposure settings panel to the bottom of the HUD and make bottom camera readouts interactive to mirror authentic real-camera EVF/LCD exposure controls. Implemented and verified.
  - Re-anchored `aside.param-deck` ("相機曝光設置") to `grid-row: 3` at the bottom center of the HUD (`width: min(980px, 96vw)`), sliding up smoothly from the bottom edge overlaying the bottom control area when activated (via button click or shortcut `P`).
  - Added close button (`✕`) to `param-deck` header and reorganized deck body into a 3-column real-camera status & control layout (`deck-grid`).
  - Converted static exposure readouts in `.dock-readout` (快門, 光圈, ISO, 補償) into interactive buttons (`.dock-btn`) that trigger exposure deck controls directly from the bottom bar.
  - Updated responsive media queries in `app/globals.css` so exposure settings remain bottom-docked across wide, medium, and mobile display breakpoints.
  - Validation: `cmd /c npm test` passed all 13/13 unit and product tests, production build (`vinext build`) completed cleanly, and `git diff --check` passed without whitespace warnings.

- New local objective on 2026-08-07: filter available camera lens options to strictly include 24-70, 70-200, 200-600, and 400-800 zoom lens models, and synchronously update field-of-view (視角 FOV) across 3D camera projection and HUD readouts when changing focal lengths. Implemented and verified.
  - Updated `LENSES` array in `app/page.tsx` to contain exclusively four zoom lens options: `24–70 mm F2.8 標準變焦`, `70–200 mm F2.8 遠攝變焦`, `200–600 mm F5.6–6.3 超遠攝變焦`, and `400–800 mm F6.3–8.0 超遠攝變焦`.
  - Added synchronous 3D camera FOV update in `app/viewport.tsx`: during render execution, `engineRef.current.aimCamera()` is triggered synchronously on `viewRef.current` update so `camera.fov = verticalFieldOfView(takingFocal, camera.aspect)` and `camera.updateProjectionMatrix()` refresh immediately without frame lag.
  - Added real-time horizontal FOV readouts (`視角 ${horizontalFieldOfView(focal).toFixed(1)}°`) to EVF top status bar, parameter deck lens card, and focal length slider helper text.
  - Validation: `cmd /c npm test` passed all 13/13 unit and product tests, production build succeeded cleanly, and `git diff --check` passed without whitespace issues.

- New local objective on 2026-08-07: fix Scene 5 (group photo / 多人合影) character overlapping bug and arrange group members in staggered front & back rows. Implemented and verified.
  - Root cause: `poseSubjects()` and `capture()` in `app/viewport.tsx` contained an early return `if (built.key === "group") return;`. Because billboard meshes are initialized at `[0, height/2, -6]` during scene assembly in `app/scene3d.mjs`, skipping position updates for group subjects caused all group members to remain stacked at `(0, y, -6)` overlapping each other.
  - Removed early returns for `built.key === "group"` in `poseSubjects()` and `capture()` loops in `app/viewport.tsx`, enabling `subjectPath()` to set distinct 3D world coordinates and contact shadows for every group member.
  - Updated `MULTI_ROUTES.group` in `app/motion.mjs` to define 7 distinct, non-overlapping positions arranged in two staggered depth rows: a front row (Z = -5.4m to -5.5m at X = -1.4, 0, 1.4) and a back row (Z = -6.6m to -6.8m at X = -2.1, -0.7, 0.7, 2.1). This ~1.2m depth difference between rows enforces the aperture group-photo lesson ("每一排人的臉都要在景深範圍內").
  - Updated `buildScene` in `app/scene3d.mjs` to generate 7 unique, non-repeating Q-version character textures for the group photo members.
  - Validation: `cmd /c npm test` passed all 13/13 unit and product tests, production build succeeded cleanly, and `git diff --check` passed without whitespace issues.

- New local objective on 2026-08-07: set default camera mode fixed to P mode (程式自動曝光). Implemented and verified.
  - Updated default state in `app/page.tsx` from `useState<Mode>("A")` to `useState<Mode>("P")`.
  - Added unit test assertion in `tests/rendered-html.test.mjs` verifying default mode state is `"P"`.
  - Validation: `cmd /c npm test` passed all 13/13 unit and product tests, production build succeeded cleanly, and `git diff --check` passed without whitespace issues.

- New local objective on 2026-08-07: perform thorough copyright and trademark compliance audit and replace all proprietary brand names, trademarks, and model designations with generic photography terms. Implemented and verified.
  - Removed trademarked Sony model names and branding labels: replaced `ILCE-1` with `CAM-PRO 1`, `α1 曝光設置` with `相機曝光設置`, and `SONY α1 TRAINER` with `PRO CAMERA SIMULATOR`.
  - Replaced proprietary Sony lens line trademarks (`FE`, `GM`, `GM II`, `G OSS`) with descriptive generic lens names: `24–70 mm F2.8 標準變焦`, `50 mm F1.2 大光圈定焦`, `85 mm F1.4 人像定焦`, `70–200 mm F2.8 遠攝變焦`, and `200–600 mm F5.6–6.3 超遠攝變焦`.
  - Replaced proprietary `E 接環` label in UI lens section with generic `通用接環`.
  - Updated test assertions in `tests/rendered-html.test.mjs` to match the non-infringing generic labels.
  - Validation: `cmd /c npm test` passed all 13/13 unit and product tests, production build succeeded cleanly, and `git diff --check` passed without whitespace issues.

- New local objective on 2026-08-07: fix autofocus raycasting so it penetrates 3D vehicle, animal, and character groups, correctly detecting target distances for cars, taxis, scooters, deer, dogs, cats, and birds. Implemented and verified.
  - Root cause: `raycaster.intersectObjects(built.scene.children, false)` in `app/viewport.tsx` was previously using `recursive = false`. Because 3D vehicles (cars, taxis, scooters), 3D animals (deer, dogs, cats, bunnies, birds), and character figures are structured as `THREE.Group` containers whose geometry resides in child sub-meshes, non-recursive raycasting skipped them entirely.
  - Fixed `probeFocus()` and `probeLight()` in `app/viewport.tsx` to recursively intersect scene children (`recursive = true`), filtering out `depthWrite: false` visual decals (shadows, puddle glow) and line segments (rain).
  - Added `findSubjectForHit()` in `probeFocus()`: when raycasting hits a sub-mesh belonging to a subject in `built.subjects` (e.g. a car's body, window, or wheel), `subjectM` is dynamically assigned to the exact hit/subject distance instead of falling back to `subjects[0]`.
  - Added `mesh.raycast = () => {}` to `contactShadow` decals and rain line segments in `app/scene3d.mjs` so visual effects do not intercept autofocus rays.
  - Added unit test assertions in `tests/rendered-html.test.mjs` asserting recursive raycast penetration and depthWrite filtering.
  - Validation: `cmd /c npm test` passed all 13/13 unit and product tests, production build completed cleanly, and `git diff --check` passed without whitespace issues.

- New local objective on 2026-08-07: rationalize auto-exposure (AUTO & P modes) so aperture dynamically opens to maximum aperture in night scenes and low-light environments, and fix AUTO ISO shutter speed threshold comparison. Implemented and verified.
  - Overhauled Program AE and AUTO mode solver in `app/page.tsx` (`effective` memo): aperture is no longer hardcoded to a static preference per scene.
  - Fixed shutter speed comparison logic (`iso100Shutter <= safeShutter` instead of `>=`): since shutter speeds are expressed in seconds (e.g. 1/1000s = 0.001s < 1/125s = 0.008s), faster shutter speeds have smaller numerical values in seconds. Fixing this comparison restores AUTO ISO behavior in bright daylight and low light across AUTO, P, A, S, and M modes.
  - When target EV decreases (night scenes, low light, or aiming into dark shadows), aperture dynamically opens toward `lensMaxAperture` before raising ISO or slowing shutter speed unnecessarily.
  - In night scenes (EV 5.8), AUTO and P modes now automatically open to the maximum aperture of the selected lens (e.g., F1.2 / F1.4 / F2.8) and scale AUTO ISO accurately (e.g., ISO 1600 for F2.8).
  - Added unit test assertions in `tests/rendered-html.test.mjs` asserting night scene max-aperture selection, bright-light base ISO 100 retention, and dynamic AUTO ISO scaling.
  - Validation: `cmd /c npm test` passed all 13/13 unit and product tests, production build succeeded cleanly, and `git diff --check` passed without whitespace issues.

- New local objective on 2026-08-07: set sun and moon to infinite distance (`focusM = INFINITY_FOCUS_M` / `∞`) when aimed at with autofocus. Implemented and verified.
  - Added `visibleSun.raycast = () => {}` and `moon.raycast = () => {}` to `app/scene3d.mjs` (tagging both as `isCelestial`), matching the existing `skyDome` infinity raycast bypass.
  - When players align the AF reticle with the Sun or Moon, raycasting bypasses the celestial mesh and defaults to infinity focus (`INFINITY_FOCUS_M = 4000`), displaying `∞` in the HUD reticle, status readout, and EXIF metadata.
  - Added test assertions in `tests/rendered-html.test.mjs` verifying celestial raycast bypass.
  - Validation: `cmd /c npm test` passed all 12/12 unit and product tests, production build succeeded, and `git diff --check` passed without whitespace issues.

- New local objective on 2026-08-07: add structured EXIF photo parameters to photo review, move scene selection exclusively to the main title page, remove in-scene scene rail, and add a "Return to Main Screen" button in HUD. Implemented and verified.
  - Added structured `PhotoParams` metadata to captured photos and review overlay (`.playback-exif` and `.result-params-strip`), displaying exact shutter speed, aperture, ISO, focal length, exposure compensation, focus distance, camera mode, and lens name when reviewing captured photos or library thumbnails.
  - Moved scene selection cards exclusively to the main title screen (`.start-screen`), presenting a 7-scene selector grid with scene numbers, titles, brief descriptions, and accent colors.
  - Removed the in-scene scene selector rail (`aside.scene-rail`) from the shooting scene HUD to keep shooting focus clean.
  - Added a 「🏠 主頁面」 (Return to Home) button in `.hud-actions` so players inside any scene can release pointer lock and return to the main title screen at any time.
  - Validation: `cmd /c npm test` passed all 12/12 unit and product tests, production build completed cleanly, and `git diff --check` passed without whitespace issues.

- New local objective on 2026-08-07: overhaul shutter speed, aperture, and ISO exposure brightness scaling so ISO adjustments directly scale photo brightness realistically across all manual modes and shift the photo histogram. Implemented and verified.
  - Replaced the heavily compressed `Math.pow(2, (scene.sceneEv - cameraEv) * 0.42)` with `[0.32, 2.1]` clamping in `app/page.tsx` with realistic photographic exposure scaling `brightness = clamp(Math.pow(2, exposureDelta), 0.02, 8.0)`.
  - Incorporated view-dependent light metering (`sceneLightEv = scene.sceneEv + lightRead.ev`) directly into the rendered photo brightness calculation so aiming toward key light sources or shadows affects output image exposure.
  - In manual exposure (`M`) mode, ISO adjustments from 50 to 102400 now monotonically brighten/darken photos and shift the RGB histogram left/right across the full 11-stop ISO range instead of capping at ISO 800.
  - Validation: `cmd /c npm test` passed all 12/12 unit and product tests (including new unit test for ISO/shutter/aperture exposure scaling & reciprocity), production build passed cleanly, and `git diff --check` passed without whitespace issues.

- New local objective on 2026-08-07: add RGB / Luminance photo histogram (色階分布圖) to photo review. Implemented and verified.
  - Added `app/histogram.tsx` client component that computes 256-bin pixel distributions from captured JPEG images using offscreen canvas `getImageData`.
  - Displays a Sony α1-style photo review overlay in `div.playback` with RGB+Y, Luminance Y, and RGB channel modes, 0–255 brightness axis, grid lines, and shadow/highlight percentage stats.
  - Validation: `npm test` passed 11/11 tests, `npm run build` completed cleanly without errors.

- New local objective on 2026-08-07: overhaul and re-optimize 3D scene objects and coordinates across all 7 scenes (`landscape`, `bird`, `sports`, `portrait`, `group`, `street`, `night`) in `app/scene3d.mjs`. Implemented and verified.
  - Resolved geometry defects & misalignments: fixed building facade wall clipping in `street` and `night` by re-aligning sidewalk objects (bistro cafe, vending machines, newsstand, ramen shop) onto proper sidewalk bounds (`X = -8.5` to `-7.2` & `X = 5.2` to `8.5`); re-framed 85mm interior objects in `portrait` around the sitter for bokeh focus (`X = -1.15` to `1.15`); fixed disjointed depth scale in `landscape` by shifting lake to `Z = -130`, cabin to `Z = -55`, pier to `Z = -52` & campsite to `Z = -22`; adjusted jumbotron height in `sports` (`Z = -62, Y = 15`) and lane alignment; framed group photo with street lamp bokeh in `group` (`X = ±4.5`).
  - Validation: `npm test` passed 11/11 tests, `npm run build` completed cleanly, and `git diff --check` passed.

- New local objective on 2026-08-07: add fixed physical light sources, view-dependent reflected-light metering, and visible light/shadow effects. Implemented and freshly validated after the user's correction that lights must not drift over time.
  - Each scenario now keeps its authored sun, moon, ambient light, street lamps, and neon sources at fixed positions and intensities. The earlier deterministic light cycle and moving directional light were removed because these sources should remain stable within a practice scene.
  - Every daytime world creates a real Three.js sun sphere at a fixed world-space position collinear with its `DirectionalLight`; it is not part of a background image. The night world retains a real moon mesh plus fixed point lights and emissive neon geometry.
  - The centre of the camera view meters the aimed surface's luminance, emissive contribution, and angle to the fixed key light. A visible sun/moon entering the view adds a strong highlight reading; a dark obliquely lit surface lowers it. AUTO/P/S/A and ISO AUTO therefore recompute when the photographer aims toward a different brightness, not merely because time passed.
  - The WebGL renderer now enables 1024 px PCF-filtered shadows. Opaque Lambert geometry casts and receives directional shadows; transparent billboards, sky, rain, and shadow decals remain outside that pass.
  - Fresh validation after fixing the sources: production build passed, all 11/11 product tests passed, `git diff --check` passed, and source scanning found no remaining time-cycle or elapsed-time light movement. In a live hardware-WebGL browser, a fixed landscape view stayed exactly at 1/125 s, F5.6, ISO 200 across 4.2 seconds; turning and capturing toward the real sun sphere changed the automatic shutter to 1/500 s. The captured JPEG visibly contains the sun as world geometry, rendering held 75/75 FPS at 27 draw calls, and the console was clean.

- New local objective on 2026-08-07: support hardware-accelerated rendering and a 120 FPS live-view target. Implemented and validated within the available 75 Hz browser surface.
  - The WebGL renderer now explicitly prefers the high-performance GPU, disables the expensive preserved drawing buffer, reports WebGL/GPU/FPS/draw-call diagnostics through canvas data attributes, and caps 120 Hz rendering to a 2560x1440-equivalent pixel budget.
  - Live resolution adapts in small steps when a high-refresh display misses its inferred frame target; a 60 Hz display is not degraded while chasing frames it cannot present.
  - Static scene objects freeze their transform matrices, while moving subjects, shadows, and rain remain dynamic. Pointer-lock mouse input is coalesced to at most one React state update per animation frame.
  - First browser measurement used hardware WebGL2 on AMD Radeon/D3D11 and a 75 Hz presentation surface. Five scenes ran at 72-75 FPS, but bird fell to 66 and night to 63 even at minimum adaptive DPR; 100+ draw calls, rather than fill rate, were the limiting evidence. A second pass now merges compatible opaque static meshes into GPU batches while preserving depth layers and autofocus raycasting; this batching still needs validation.
  - The first validation after batching built successfully, then the product suite passed 9/10 and failed only because its source-contract regex required the old exact `await import("three")` spelling. The renderer now loads Three and its geometry utility together with `Promise.all`; the stale assertion was updated before retrying.
  - Static batching reduced representative live draw calls from 68 to 15 (landscape), 50 to 15 (bird), 104 to 17 (sports), 38 to 20 (group), 111 to 38 (street), and 105 to 68-69 (night).
  - Fresh production build passed, all 10/10 product tests passed, and `git diff --check` passed. `tsc --noEmit` still reports only the three pre-existing Cloudflare worker typing gaps (`cloudflare:workers`, `Fetcher`, and `D1Database`). ESLint remains at 17 problems / 14 errors; none point to the new renderer, performance policy, batching code, or pointer-input coalescing.
  - Fresh-browser validation: WebGL2 reported hardware rendering through ANGLE on AMD Radeon/D3D11. All seven scenes loaded without console warnings; after scene warm-up the available 75 Hz surface held 75 FPS, including bird and the heaviest rain/night scene. Night used adaptive DPR 0.875 and 68 draw calls. A post-batching shutter press produced a playback image and a clean console.
  - Not verified: an actual 120 Hz presentation surface was unavailable, so this proves hardware acceleration, 120 FPS-capable scheduling/policy, and a steady 75/75 FPS on the test display, not a measured 120 FPS on this machine.

- New local objective on 2026-08-07: add ISO AUTO and remove the mechanical/electronic shutter setting. Implemented; validation is pending.
  - Removed `ShutterType`, its settings buttons, the EVF shutter-type label, and the mechanical 1/8000 restriction. The shutter control now always exposes the simulator's full 1/32000–30 s range.
  - Added an ISO AUTO toggle to the desktop parameter deck and mobile ISO control. AUTO mode always uses it; P/S/A/M can switch between manual ISO 50–102400 and AUTO ISO 100–12800.
  - AUTO ISO now respects the selected exposure mode: it protects each scenario's minimum usable shutter in AUTO/P/A, fills exposure after aperture selection in S, and adjusts ISO around fixed shutter/aperture in M. Exposure compensation remains available in M only while AUTO ISO is active.
  - Validation on 2026-08-07: a fresh production build passed, all 9/9 product tests passed, `git diff --check` passed, and a source scan found no remaining shutter-type state, UI, or mechanical/electronic labels under `app`. Browser interaction was not performed.

- New local objective on 2026-08-07: improve readability because the user found the text too small. Added a scoped readability pass in `app/globals.css`: former 8–10 px HUD, settings, status, playback, and mobile labels now render at roughly 10–13 px; primary control/readout values are 17–20 px; the settings panel is slightly wider on desktop and narrow screens to prevent the larger labels from crowding. Fresh production build, all 9/9 product tests, and `git diff --check` passed. Browser layout was not inspected because the user did not request browser testing. Publishing is blocked on the repository rule requiring explicit authorization before commit/push.

- New local objective on 2026-08-07: after every shutter press, release pointer lock; keep every moving subject continuously circling the scene without disappearing; and add more birds. Implemented but validation is still pending.
  - `capture()` now calls `document.exitPointerLock()` after a successful frame, so mouse-look ends regardless of whether the shutter came from click, Space, the HUD button, or mobile controls.
  - The bird, sports, street, and night routes are now closed 3D circuits instead of one-way paths that jump from their endpoint back to their start. Tests cover exact lap closure and continuity across the seam.
  - The wildlife scene now creates a seven-bird flock with staggered lap phases and varied sizes. Live posing and shutter sub-frame posing preserve that spacing so the birds do not collapse onto one point during capture.
  - Validation on 2026-08-07: fresh production build passed, all 9/9 product tests passed, and `git diff --check` passed. Numeric coverage confirms every moving route changes Z distance by more than 5 m, closes at exactly the same 3D point, crosses its lap seam continuously, and keeps all seven birds at distinct positions. Visual/browser behavior has not been verified because it was not requested.

- New objective on 2026-08-07: the user asked for the mouse pointer to double as the focus area, and for moving the mouse to turn the view the way it does in a shooting game. Implemented locally.
  - Aiming: clicking the live view calls `requestPointerLock()` on `.live-stage`. While locked, raw `movementX/movementY` turn the tripod head at the same one-pixel-per-pixel-angle mapping the drag already used (negated, because a shooter turns toward the mouse instead of dragging the world with it), the AF frame is pinned to the centre of the picture with a crosshair, and the cursor is hidden. Escape releases the mouse and the previous drag-to-pan behaviour is still there as the fallback, including for touch. A stationary click grabs the mouse when free and fires the shutter when locked; Space still fires either way. Starting the game requests the lock, after the fullscreen transition settles when 全螢幕開始 was used.
  - Focus: the AF frame is a real autofocus point. `viewport.tsx` raycasts from the frame into the scene (`probeFocus`) and the hit distance becomes the focused plane; the sky dome is excluded (`raycast` no-op in `scene3d.mjs`) so aiming at sky means infinity. `depthBlurPlan` gained `focusM` and `subjectM`, so every depth bucket — including the subject — is blurred by its own circle of confusion relative to whatever the frame is sitting on, and a walking or flying subject is judged at its live distance rather than the scenario's nominal plane. When the pointer is free the AF frame follows the cursor (tap-to-focus on touch); when locked it is the crosshair.
  - Consequences: `capture()` returns `{image, focusM, subjectM}`, the settings line records the focus distance, the status readout gained a 主體合焦/主體失焦 pill that names the distance, the reticle turns green when the subject is sharp, and coaching gained a front/back focus note. Scoring was reweighted from exposure .4 / motion .3 / aperture .2 / focal .1 to exposure .35 / motion .25 / focus .2 / aperture .12 / focal .08, because an out-of-focus frame could otherwise still score 94. This partially reverses the 2026-08-07 focus removal below: no AF-S/AF-C selector and no manual distance dial came back, and the source-contract test still forbids both.
  - Two real defects were found and fixed during browser verification: (1) `probeFocus` raycast against world matrices that only `renderer.render()` refreshes, so before a scene's first frame every object measured as sitting on the tripod and the AF read 0.1 m — the probe now calls `scene.updateMatrixWorld()` and `aimCamera` calls `camera.updateMatrixWorld()`; (2) the capture probed the AF frame before re-posing the subject to the instant the shutter fired, so a moving subject was measured one frame stale — `poseSubjects` now runs first.
- Validation on 2026-08-07: fresh production build passed, 8/8 product tests passed (two new ones cover the AF plane and the moving-subject plane), no new `tsc --noEmit` errors (the same three pre-existing `cloudflare:workers` / `Fetcher` / `D1Database` worker typings), and ESLint is 16 problems / 14 errors against the 17 / 15 baseline.
- Live browser verification on 2026-08-07: on the portrait scenario at 85 mm f/1.4, aiming at the sitter reported 對焦 4.2 m, 主體合焦 and scored 98; aiming at the back wall reported 6.6 m with the 對焦落在主體後方 note at 78; aiming at the foreground curtain reported 1.4 m with the 對焦落在主體前方 note at 78. Measured on the captured JPEGs themselves, mean horizontal gradient over the subject region fell from 25.8 to 2.4 (and file size from 156 kB to 71 kB) when focus moved from the sitter to the wall, so the AF point really drives the optics. On landscape, three aim points returned 26 m / 478 m / ∞ and the HUD, settings line and reticle colour all followed. A fresh tab loaded with a clean console.
- Not verified: pointer lock itself. The automation pane does not composite, so its document is `hidden`, `requestAnimationFrame` never fires and Chrome refuses the lock; the live-view probe loop and the shooter mouse-look could not be exercised there. Everything above was measured through the capture path, which runs synchronously off the click. The Chrome extension was not connected, so a visible window was unavailable.
- Previous objective on 2026-08-07: the user asked for the game scenes to become 3D "like a shooting game". They chose a real 3D world with the existing Q-version cutouts kept as billboard subjects, and a tripod-style look (turn in place, no walking). Implemented locally with `three@0.185.1` (a new runtime dependency; `@types/three` added as a dev dependency).
  - `app/scene3d.mjs` builds all seven scenarios as procedural low-poly geometry (dawn valley with snow-capped ridges and a lake, wetland with reed banks, stadium with a raked stand and floodlights, interior room with a window wall, plaza, daytime street, neon rain city). Building facades use procedurally generated `CanvasTexture` window grids, and the night facades reuse that map as an `emissiveMap` so windows glow. `THREE` is passed in as an argument so the renderer never enters the server bundle.
  - `app/world.mjs` holds per-scenario world setup (camera height, focus plane, near/far depth-band distances, sky, fog, lighting, look limits) plus real optics: `circleOfConfusionMm` / `defocusBlurPixels` / `depthBlurPlan`. Depth of field is now derived from the actual circle-of-confusion formula rather than an invented curve.
  - `app/viewport.tsx` owns the `WebGLRenderer`, the tripod head, and the capture pipeline. Live view is a single clean pass over all layers. A capture renders the scene N times across the exposure into three depth buckets (`LAYER.far` / `subject` / `near`), blurs each bucket by its own circle of confusion, then applies exposure and ISO grain, and returns a JPEG data URL. Photos are now real images, so the library and playback show actual captured frames instead of re-simulated CSS.
  - `app/motion.mjs` was rewritten from 2D percentage offsets to world-space 3D routes (`subjectPath`) plus `exposureSamples`, which supplies the sub-frame poses that produce motion blur.
  - `app/optics.mjs` gained `verticalFieldOfView`; the now-unused `focalLengthToSceneScale` and `REFERENCE_FOCAL_LENGTH_MM` were deleted because real perspective supersedes the CSS zoom ratio.
  - `page.tsx` drag now turns the camera one pixel-angle per dragged pixel instead of panning a background, so aiming a 600 mm lens is as tight as it would be through a real finder. The entire `SceneArtwork` CSS layer stack and its styles were removed.
- Three real defects were found and fixed during browser verification, all confirmed by re-testing: (1) canvas accumulation used `source-over` at alpha 1/N, which converges to `1-(1-1/N)^N` and darkened every photo to ~66% — switched to additive `lighter`; (2) a fixed sub-frame cadence produced countable ghosts instead of a smear, so sample count is now derived from the subject's actual projected on-screen travel plus shake; (3) the simulation clock accumulated clamped frame deltas and drifted behind wall-clock, so a throttled frame could leave the subject out of the shot — it now reads wall-clock, and `capture()` re-reads it at the moment the shutter fires.
- Validation on 2026-08-07: fresh production build passed, 7/7 product tests passed, `git diff --check` passed, no app-level `tsc --noEmit` errors (the three remaining errors are the pre-existing `cloudflare:workers` / `Fetcher` / `D1Database` worker typings), and ESLint improved to 17 problems / 15 errors from the 29 / 17 baseline.
- Live browser verification on 2026-08-07: all seven scenarios build (25–215 meshes each) and render; all five billboard textures load; captures succeed in all seven scenarios with real JPEG output and populated library thumbnails; dragging visibly turns the head with correct near/far parallax; no page scroll and zero HUD cluster overlaps at 1280×720; console clean on a fresh load. Visually confirmed via rendered contact sheets: sharp-to-the-horizon stopped-down landscape, f/1.4-style portrait separation with a defocused foreground curtain, a smooth continuous smear at 1/15 s versus a frozen sprinter at 1/2000 s, and a motion-blurred bird at 1/30 s versus frozen at 1/4000 s.
- Note: the automation pane still does not composite frames, so `computer` screenshots remain unavailable. Verification used offscreen WebGL renders posted to a temporary local sink plus DOM/geometry probes; that scaffolding has been removed.
- Deployment is pending explicit authorization to commit/push under `AGENTS.md`.

- Focus removed on 2026-08-07 at the user's request: the game is only meant to practice the exposure triangle. Deleted the `FocusMode` type and AF-S/AF-C/MF selector, the manual focus-distance control, the `scene.distance` field, the viewfinder focus box, the out-of-focus subject blur, the focus status readout, and both focus coaching notes. Capture scoring was reweighted from exposure .3 / motion .25 / focus .25 / focal .1 / aperture .1 to exposure .4 / motion .3 / aperture .2 / focal .1, and the third status chip now reports depth of field instead of focus lock. Source contract tests now assert the absence of focus code and CSS. A fresh production build and 4/4 tests passed; live browser check confirmed no focus UI, no console errors, and a working capture (score 91 on the landscape scene).

- Focal-length/FOV correction completed locally on 2026-08-07: replaced the previous bounded logarithmic visual zoom with full-frame rectilinear field-of-view geometry referenced to 24 mm. Numeric coverage verifies the Sony alpha 1 35.9 mm sensor-width horizontal angles and 24/50/200 mm crop ratios. A fresh production build, 4/4 product tests, and `git diff --check` passed. Live browser interaction verified standard-lens scale ratios of 1.000, 1.458, 2.083, and 2.917 at 24/35/50/70 mm, and wildlife-lens CSS targets of 8.333, 12.5, 16.667, and 25.0 at 200/300/400/600 mm; the browser console had no errors or warnings. Publishing remains pending explicit authorization.

- Sony α1 exposure implementation passed a fresh production build and 2/2 product tests on 2026-08-07. The stale rendered-heading assertion was corrected, exposure compensation now retains exact 1/3 EV internal steps, and `git diff --check` passed.

- New Sites project initialized on 2026-08-07. Dependencies installed successfully.
- The starter has been replaced with a seven-scenario playable camera simulator.
- The user clarified that live view must remain visually normal; photographic effects appear only after shutter release.
- The user requested left-click shutter release, mouse-wheel zoom, interchangeable lenses, and a Sony-style information layout.
- The user additionally required AF-S/AF-C autofocus plus manual focus, and clearly separated foreground, subject, and background planes so aperture-driven depth of field is visually obvious.
- The virtual scene must extend beyond the viewfinder at every focal length; 24 mm wide angle must never reveal a scene boundary.
- Add an in-camera photo library that preserves and replays earlier shots with their original scene, settings, score, and rendered effects.
- Support click-and-drag camera panning independently from mouse-wheel focal-length zoom; a stationary click releases the shutter.
- Animate birds, athletes, and people continuously in live view so shutter-speed choices have an observable subject-motion consequence; freeze the captured playback frame and apply calculated motion blur.
- A bespoke social preview image was generated, visually checked, saved as `public/og.png`, and wired into request-host-derived Open Graph/X metadata.
- The starter preview files and `react-loading-skeleton` dependency have been removed.
- First production build attempt failed before compilation because the generated npm scripts used Unix-style inline environment-variable syntax under Windows. The scripts need a cross-platform correction before retrying.
- Moving subject positions were initially CSS-animation-only, so captured playback could instantiate the bird off-frame. Subject position must be part of the immutable photo snapshot.
- Build scripts were corrected to run cross-platform. A full production build then completed successfully on 2026-08-07.
- The generated starter tests were replaced with product-specific server-render and source-contract tests; these still need to be run.
- Product-specific tests passed 2/2 after a fresh production build on 2026-08-07.
- A private Sites project was created and its opaque project ID was persisted in `.openai/hosting.json`.
- Completion audit found the 24 mm canvas could still expose an edge at maximum drag. The scene was expanded to a large panorama and panning was constrained to a safe range; this change still needs a fresh test run.
- The wide-angle panorama safety fix, dynamic subject snapshot, photo library, camera modes, autofocus modes, lens switching, drag pan, wheel zoom, and product metadata all passed a fresh production build and 2/2 tests. `git diff --check` also passed.
- The user explicitly authorized creating the release commit and publishing the site on 2026-08-07.
- The first commit attempt was stopped by `git diff --cached --check` because `AGENTS.md` had an extra blank line at EOF. The whitespace issue was corrected before retrying; no commit was created by that attempt.
- Release commit `54f4612` was created and pushed successfully to the private Sites source repository.
- The first packaging attempt failed because Git Bash `tar` interpreted a `C:/...` archive path as a remote archive target. Retry with `/c/...` paths; the validated source and build were not changed.
- The path issue was corrected with `/c/...` Git Bash paths. Version 1 was saved from commit `8f37f2e`, privately deployed successfully, and opened at `https://aperture-world.gjhcy4gc52.chatgpt.site` on 2026-08-07.
- New objective on 2026-08-07: calibrate adjustable exposure controls to the original Sony α1 (ILCE-1), using Sony's official specification/help guide as the authority.
- Confirmed α1 still-photo reference limits: AUTO/P/A/S/M exposure modes; ISO 100–32000 with expanded ISO 50–102400 and AUTO ISO 100–12800; mechanical shutter 1/8000–30 s plus Bulb; electronic/Auto shutter up to 1/32000; exposure compensation ±5 EV (body dial ±3 EV); and Multi, Center-weighted, Spot, Entire Screen Average, and Highlight metering.
- Implemented α1-calibrated 1/3-step shutter speeds through 1/32000–30 s, ISO 50–102400, AUTO ISO limits, ±5 EV compensation, shutter-type limits, five metering modes, and lens-specific aperture ranges.
- Overhauled all 7 scenario visual art assets into high-resolution Q-version (chibi/kawaii anime illustration) artwork (`public/scenes/*.jpg`).
- Refactored `SceneArtwork` in `app/page.tsx` and updated `app/globals.css` to render multi-layer Q-version background & subject graphics with dynamic depth-of-field blur, motion blur, zooming, and camera panning.
- Fresh production build (`cmd /c npm run build`) and 2/2 test suites (`cmd /c npm test`) passed cleanly on 2026-08-07.
- Visual review of the redesigned Q-version artwork in the live local app found integration blockers: the 1024px subject JPGs retain opaque white/card backgrounds, sports and portrait therefore render as floating white panels, the bird subject is outside the visible default crop, and the oversized `.scene-art` canvas magnifies/crops the 16:9 backgrounds enough to lose composition and apparent sharpness. The source art itself is visually strong; asset transparency, subject placement, and background canvas scaling need correction before release.
- Artwork integration fix in progress: generated and alpha-validated transparent PNG subjects for bird, sports, portrait, street, and night scenes; created clean subject-free backgrounds for portrait, street, and night; removed real Olympic/Tokyo branding from the sports background; changed focal-length rendering to a bounded logarithmic visual zoom; reduced wide-angle pan limits; and added independent street/night subject animation. Live and captured visual verification still pending.
- First post-artwork production build succeeded. The first product-test run passed server rendering but failed the new asset-existence assertion because its test URL incorrectly traversed one directory above the project (`../public` relative to an already-rooted base). The test path was corrected to `public/scenes`; product behavior was not implicated. Fresh validation pending.
- Artwork integration completed and verified on 2026-08-07. Fresh production build and 2/2 product tests passed; `git diff --check` passed; all five subject assets were verified as RGBA with alpha extrema 0–255; live visual review confirmed no white cards or visible green fringe; sports captured playback froze the subject and applied the calculated background blur; and geometric browser checks confirmed bird, sports, portrait, street, and night subject bounds all remained inside the viewfinder at the tested state.
- The user authorized the online update. Release commit `6e4cac1` was pushed, Sites version 2 was saved from that exact commit, and the private production deployment succeeded at `https://aperture-world.gjhcy4gc52.chatgpt.site` on 2026-08-07.
- New mobile/fullscreen/motion objective was implemented locally on 2026-08-07. The page now has a touch-first fixed mobile control console for shutter, aperture, ISO, focal length, shutter release, and fullscreen; the fullscreen control uses the browser Fullscreen API with an immersive fallback. Bird, sports, street, and night subjects now follow distinct two-dimensional paths with scene-appropriate vertical movement, rotation, and perspective scaling, and captures preserve the full motion pose. Motion generation was extracted into a deterministic module and a numeric test now proves every moving scene changes horizontal position, vertical position, body angle, and perspective scale while staying within safe route bounds. A fresh production build, 3/3 product tests, and `git diff --check` passed. Browser UI interaction was not performed because it was not requested, and deployment is pending explicit authorization to commit/push under `AGENTS.md`.

- New objective on 2026-08-07: the user asked for the interface to become the whole shooting window rather than a boxed viewfinder in the middle, with every parameter control placed inside the picture, playable in fullscreen, and presented like a Flash/`.io` browser game. Implemented locally: the page is now a fixed `100dvh` game shell whose live view fills the entire viewport; the old `topbar`/`workspace`/`camera-body`/`control-panel` document flow was replaced by an absolutely positioned `.live-stage` plus a non-scrolling `.hud` grid of translucent overlay clusters (brand + mission card, centre mode dial and EVF status line, progress/session-score chip, scene rail, right-hand `α1 曝光設置` slide-over deck, bottom exposure dock with the shutter release, status pills, and lens hint). Added an `.io`-style title screen with 全螢幕開始 / 直接遊玩 entry, a keyboard legend, and Space/Enter to start; a session best-score and shot counter; and `F` fullscreen, `H` hide-HUD, `P` parameter-deck shortcuts. Page-level scrolling is disabled so the game never scrolls out of frame.
- Layout was verified geometrically in a live browser at 1440×900 and 375×812: no page scroll at either size, the live stage fills the viewport, the parameter deck scrolls internally (524 px visible of 1289 px), and all HUD cluster pairs were checked for intersection with zero overlaps. Shutter release, playback return, scene switching with accent change, M-mode control enabling, deck/HUD toggles, photo-library capture, and score/session counters were all exercised successfully. Hidden panels use `visibility:hidden` so they leave the tab order.
- Validation on 2026-08-07: fresh production build passed, 3/3 product tests passed, and `git diff --check` passed. ESLint reports 29 problems (17 errors) against a pre-change baseline of 31 problems (19 errors) on the same command, so no new lint regressions were introduced; the remaining errors are the project's pre-existing ideographic-whitespace, label-association, and unused-expression patterns.
- Note: browser screenshots could not be captured because the automation pane was not compositing frames; verification was done through DOM geometry and computed styles instead. CSS transitions appear frozen under that condition, which is a measurement artifact, not a product defect.

- New objective on 2026-08-07: overhaul subjects, animals, vehicles, and motion routes across all 7 scenes as requested by the user.
  - Scene 1 (`landscape`): Add animals (deer, soaring birds, rabbits) with independent movement routes.
  - Scene 2 (`bird`): Assign independent 3D flight routes for each bird instead of sharing a single loop.
  - Scene 3 (`sports`): Add more sprinters/athletes in separate track lanes with staggered routes.
  - Scene 4 (`portrait`): Remove the foreground pillar/curtain post obstructing the camera line-of-sight (`Z = -1.4`), and add a cat subject on the armchair with motion.
  - Scene 5 (`group`): Ensure character models are non-repeating by generating unique Q-version character assets for 5–7 group members.
  - Scene 6 (`street`): Add more pedestrians, vehicles (cars, bicycles, delivery vans), and animals (dogs, cats, pigeons) with independent movement routes.
  - Scene 7 (`night`): Add more pedestrians, vehicles (taxis, scooters), and animals (night cats, dogs, owls) with independent movement routes.
  - All scene animals: Ensure every animal across all 7 scenes has its own independent movement route.
  - Implemented & verified on 2026-08-07:
    - Updated `app/motion.mjs` with `MULTI_ROUTES` and per-subject route resolution (`subjectPath(scene, seconds, phaseOffset, subjectIndex)`).
    - Updated `app/scene3d.mjs` with procedural canvas generators for Q-version character variations, animals (deer, dogs, cats, birds, rabbits), and vehicles (cars, taxis, scooters, bikes).
    - Removed the camera-blocking foreground curtain pillar in Scene 4 (`portrait`).
    - Added 5 unique, non-repeating Q-version characters for Scene 5 (`group`).
    - Aligned 3D animal and vehicle models in `app/viewport.tsx` to rotate along their travel heading (`pose.yaw`), and updated `app/motion.mjs` with linear route support (`type: "line"`). Vehicles (cars, taxis, scooters) drive strictly on the asphalt road lanes (`X = +2.6` right lane, `X = -2.6` left lane), while animals and birds fly/walk along natural linear paths entering and exiting beyond camera framing boundaries (`X = ±45` to `±60`).
    - Validation: `cmd /c npm test` passed all 12/12 unit and product tests, production build succeeded, and `git diff --check` passed cleanly without whitespace issues.


## Product Scope

- Scenarios: landscape, birds, sports, portrait, group photo, daylight, and night, each built as a real 3D world (procedural low-poly geometry) with the cute Q-version characters kept as camera-facing billboards inside it.
- The camera is a tripod: it turns in place and zooms, and does not walk.
- Camera modes: Auto, Program AE, Shutter Priority, Aperture Priority, and Manual.
- Simulate exposure, motion blur, depth of field, noise, focal length, focus distance, and contextual coaching.
- Keep live view clean and render photographic effects only in captured-photo review.
- Present the product as a full-window browser game: the live view is the entire screen, every parameter control is an overlay inside that picture, and the game is playable fullscreen from a title screen.
- Support click-to-shoot, wheel zoom, lens selection with lens-specific focal ranges, keyboard, touch, and responsive layouts.
- Render near, subject, and far depth buckets separately in captured review; the lens focuses on whatever the AF frame is aimed at, and the AF frame is the mouse pointer.
- Aim like a shooting game: the pointer is locked to the view, mouse movement turns the head, and the crosshair is the focus point.

## Next Steps

1. Verify pointer-lock aiming in a visible browser window (the automation pane cannot, see above): click the live view, confirm the mouse turns the head, that Escape releases it, and that the live-view AF readout tracks the crosshair while the world animates.
2. Obtain explicit user authorization to commit/push the validated 3D world, fullscreen, motion, focal-length/FOV, and AF-aiming changes, as required by `AGENTS.md`, then publish through Sites. Note the release now ships `three` in the client bundle, which pushed a chunk past Vite's 500 kB warning threshold.
3. If the user requests browser testing, verify mobile-width quick controls, shutter, fullscreen entry/exit, path geometry, and captured-pose freeze before or after publication.
4. Record the exact commit and deployment evidence after publication.
